entity Collection in Workspace {
    id: String,
    owner: Account,
};

namespace Collection {
    action get appliesTo {
        principal: Account,
        resource: Collection,
    };

    action update appliesTo {
        principal: Account,
        resource: Collection,
    };
}